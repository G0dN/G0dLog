# G0dLog 需求基线

> 状态：已确认
> 确认日期：2026-09-05
> 本文档记录需求访谈后的共同结论，是后续设计、实现和验收的需求基线。若本文档与现有 `REQUIREMENTS.md`、`README.md`、原型代码或旧部署配置冲突，以本文档为准。旧文件在实施阶段再按本文档迁移，不应被当作仍然有效的产品决策。

## 1. 项目目标与边界

G0dLog 是供站主和受邀朋友共同写作的小型多作者博客。它不是开放注册的内容平台，也不追求大规模社区功能。

核心目标：

- 为公开文章提供稳定、克制、专注的阅读体验；
- 由站主掌控账号、数据、部署和恢复；
- 正式环境完全运行在极空间 Z4S NAS 上；
- 在小规模使用场景下保持可靠、容易维护和可恢复；
- 项目代码和容器镜像公开，采用 MIT License。

预期规模较小，作者和更新频率都不高。正式环境接受单台 NAS 的尽力可用，不承诺 SLA，也不建设第二台在线服务器。

## 2. 品牌与公开信息

- 正式名称：**G0dLog**。
- 项目地址：<https://github.com/G0dN/G0dLog>。
- 可公开联系邮箱：`gordon_official@163.com`。
- 首页介绍的原始含义是邀请访客支持项目并联系站主，最终措辞和排版可以调整，但不得堆砌说明文字。
- `SSBS Gordon Li 10-5` 属于非公开信息，不得出现在页面、RSS、公开接口、元数据、搜索索引、示例数据或导出内容中。

联系邮箱只允许出现在网页 HTML 中：

- 使用 Cloudflare Email Address Obfuscation 降低普通抓取风险；
- 不在 RSS、公开 API、结构化元数据或内容导出中暴露；
- 不将混淆功能描述为对高级抓取器的绝对保护。

## 3. 用户、角色与资源关系

### 3.1 公网访客

- 无需登录即可浏览主页、专栏、文章和作者页面；
- 可以使用全站搜索和专栏内搜索；
- 不具备任何写入权限。

### 3.2 Owner

- 系统中只有一个 Owner；
- 负责创建、启用、停用 Author 账号以及重置密码；
- 可以管理全部专栏、协作者、文章、版本和媒体；
- 可以恢复所有可恢复内容；
- 可以明确永久清除误上传、涉及隐私或违法违规的媒体；
- 不设置第二管理员，也不设置 Editor 角色。

Owner 必须能够在 NAS 上通过 CLI 离线重置自己的密码，避免邮件或网页登录故障造成永久失去管理权限。

### 3.3 Author

- 账号只能由 Owner 创建，不开放自行注册；
- 可以维护自己的公开资料；
- 可以创建专栏；
- 可以在自己创建或受邀协作的专栏内创建文章；
- 在仍有专栏权限时，可以直接发布自己创建的文章，无需审核流程。

### 3.4 专栏创建者与协作者

“专栏创建者”和“专栏协作者”是资源级关系，不是全局角色。

- 专栏创建者可以编辑专栏信息、邀请或移除协作者、管理专栏内全部文章并调整文章顺序；
- 协作者默认只能管理自己创建的文章；
- 协作者被移除后，其既有公开文章及署名继续保留，但本人不能再编辑这些文章；
- Owner 始终可以管理全部资源。

所有权限都必须在服务端校验，不能只依赖前端隐藏按钮。

## 4. 账号与会话

- Author 账号由 Owner 创建并分发初始凭据；
- 作者首次登录后应修改初始密码；
- 账号可以停用，停用不会删除已发布内容或署名；
- 第一版不提供公开注册、邮件找回密码和双重验证；
- `/studio` 可以通过公网 HTTPS 登录；
- 密码、会话和登录接口采用成熟、适度的安全措施；
- 密码重置后，其他既有会话应失效。

安全要求以结果为导向，包括强密码策略、登录限速、安全的 HttpOnly/SameSite Cookie、Origin/CSRF 防护和合理的会话期限。实现时不应为了表面上的“安全完整”堆叠大量缺乏依据的常量、默认值和复杂分支。

## 5. 内容模型与工作流

### 5.1 专栏

- 每篇文章必须属于一个专栏；
- 不需要显式归入专题的文章进入系统默认专栏“随笔”；
- 专栏具有稳定且不可变的内部 ID、可读 slug、标题、简介、创建者、协作者、排序及删除状态；
- 没有已发布文章的专栏不在公开主页展示；
- 删除专栏后应立即停止公开展示，但应可恢复。

### 5.2 文章

- 文章状态只包含草稿和已发布；删除作为独立的软删除状态记录；
- 不设置投稿、待审核、驳回或 Editor 审核流程；
- 有权限的作者可以直接发布自己的文章；
- 已发布文章更新后继续公开，并产生永久发布快照；
- 删除后立即从公网、搜索、RSS 和 sitemap 中消失，但保留恢复能力。

文章至少应包含稳定 ID、所属专栏、作者、标题、Markdown 正文、slug、状态、首次发布时间、最后更新时间、排序信息、版本号和删除审计信息。

### 5.3 URL 稳定性

文章地址采用稳定 ID 与可读 slug 的组合，例如：

```text
/articles/<stable-id>/<readable-slug>
```

专栏和作者使用相同原则：稳定 ID 是资源身份，slug 只负责可读性。标题或 slug 改变后：

- 旧地址仍然有效；
- 旧地址重定向至当前规范地址；
- 页面输出正确的 canonical URL；
- 任何显示名称变化都不能破坏数据关联。

## 6. 语言

- 产品界面使用中文；
- Markdown 正文可以使用任意语言；
- 第一版不增加内容语言字段、翻译关系、多语言路由或语言切换器；
- 搜索必须能处理中文和英文的基本子串匹配。

## 7. 阅读体验与视觉原则

公开阅读体验是最高优先级的视觉目标。

文章页应像在浏览器中直接打开 PDF 一样安静、稳定和专注：正文是页面的绝对主体，除必要导航、菜单和阅读操作外，不展示干扰内容。

具体原则：

- 使用适合长文阅读的正文宽度、字号、行距、段落间距和标题层级；
- 桌面端和移动端都应保持清晰、稳定的阅读节奏；
- 只保留返回、目录、搜索及确有必要的操作；
- 移动端将专栏目录或文章目录收纳为按钮或抽屉；
- 避免侧栏广告式区块、营销卡片、重复标题、无意义徽章、解释性口号和持续遮挡正文的悬浮控件；
- 不在界面中反复使用“极简”等自我描述来代替真正的设计取舍；
- “像 PDF”指内容优先、信息密度和阅读专注度，不要求纸张阴影等拟物效果；
- 后台以操作效率为主，但仍需克制、清楚，避免原型式功能堆叠。

## 8. 主页、作者页和导航

### 8.1 主页

- 展示公开且未删除的专栏和最近文章；
- 专栏默认按最近一次发布文章的时间排序；
- 提供全站搜索入口；
- 展示精简的站点介绍和项目支持入口；
- 不使用大量卡片、标签或解释文案制造视觉噪音。

### 8.2 作者页

作者公开页可以展示头像、显示名称、简介、创建的公开专栏和已发布文章。账号登录名及内部管理信息不得公开。

### 8.3 文章阅读页

- 展示文章标题、作者、首次发布时间、最后更新时间和 Markdown 正文；
- 提供当前专栏的文章目录；
- 根据 Markdown 标题生成页内目录；
- 提供当前专栏范围内的搜索；
- 专栏文章默认按首次发布时间排列，后续编辑不改变默认位置；
- 专栏创建者和 Owner 可以保存人工排序。

## 9. 搜索

全站搜索覆盖：

- 文章标题；
- 文章正文；
- 作者；
- 专栏。

专栏内搜索只返回当前专栏的公开文章，并匹配标题、正文和作者。

第一版搜索要求：

- 支持中文和英文子串匹配；
- 只返回已发布、未删除的公开资源；
- 显示命中高亮及正文附近的简短摘要；
- 排序规则保持简单、可解释：标题命中优先，其次正文命中，再按时间排序；
- 不引入语义搜索、拼写纠正或外部搜索服务。

## 10. Markdown 编辑器与渲染

### 10.1 编辑方式

- 使用可靠的 Markdown 源码编辑器；
- 默认采用左右并排的源码与预览布局；
- 不实现 Typora 式逐块所见即所得编辑器；
- 明确显示保存中、已保存和保存失败状态；
- 页面离开前若仍有未保存内容，应给予清楚提示。

### 10.2 Markdown 能力

第一版支持：

- 标题、段落、列表、引用、链接、表格和图片；
- fenced code block 及语言标记；
- 自托管的代码语法高亮资源；
- 行内公式 `$...$`；
- 块级公式 `$$...$$`；
- 自托管的 KaTeX 资源；
- 自动生成文章目录。

第一版不支持：

- Mermaid；
- 通用文件附件；
- 不受控的原始 HTML。

Markdown 渲染必须清理危险 HTML，并限制链接和资源 URL 协议，防止 XSS 和危险跳转。

## 11. 自动保存、版本与并发

### 11.1 自动保存

- 用户停止输入约 3 秒后自动保存；
- 持续输入时，最长约 30 秒触发一次保存；
- 保存失败时不得静默丢失内容；
- 浏览器本地保留尚未成功提交的草稿，以便刷新、网络故障或会话中断后恢复。

### 11.2 版本历史

- 每篇文章滚动保留最近 20 个普通自动保存版本；
- 每次正式发布产生永久发布快照，不受 20 个普通版本限制；
- 删除文章时产生并永久保留最终快照；
- 有权限的用户可以查看和恢复历史版本；
- 恢复历史版本不应破坏此前永久快照。
- 2026-09-11 调整：回溯只将所选历史内容恢复到当前工作稿，不再像原实现一样新增普通自动保存快照，也不触发历史记录裁剪。并发版本号仍递增，并记录恢复审计；因此快照版本号可以不连续。已发布文章须再次明确发布才改变公开内容。回溯成功清理本地旧草稿，后续实际编辑仍正常自动保存。

### 11.2.1 专栏信息自动保存（2026-09-11）

- 专栏标题和简介停止输入约 1 秒或离开输入框时自动保存，移除「保存专栏」按钮。
- 显示待保存、正在保存、已保存和失败状态；失败保留输入并提供重试，本地保留待提交内容。
- 同一表单的保存请求串行执行，保存期间继续输入的内容随后提交；切换专栏时提交待保存内容。

### 11.3 并发编辑

- 使用版本号或等价机制检测并发写入；
- 后保存者不得静默覆盖他人已保存的新内容；
- 冲突发生时保留当前用户输入，并允许复制内容或重新加载最新版本。

## 12. 图片与媒体

- 支持粘贴、拖拽和文件选择上传图片；
- 单个文件最大 10 MB；
- 必须检查文件魔数，不能只相信扩展名或浏览器声明的 MIME；
- 清除 EXIF、GPS 等隐私元数据；
- 为公开展示生成 WebP/AVIF 衍生版本；
- 保留原图，但原图只允许从有权限的后台下载；
- GIF 不强制转码，以免破坏动画；
- 存储键和文件名必须安全且不可预测；
- 删除文章、移除引用或淘汰普通版本时，不自动物理删除媒体；
- 只有 Owner 才能对误上传、隐私或违法违规媒体执行明确的永久清除操作。

## 13. 公开输出与第一版边界

第一版需要提供：

- RSS；
- Open Graph 元数据；
- `sitemap.xml`；
- `robots.txt`。

第一版明确不做：

- 评论；
- 点赞；
- 关注；
- 产品邮件通知；
- 访问统计；
- 私密文章或私密专栏；
- 开放注册；
- 邮箱找回密码；
- 双重验证；
- 通用附件；
- Mermaid；
- 多语言 CMS。

## 14. 数据持久化

### 14.1 正式 NAS 环境

正式环境的应用、SQLite 数据库和媒体文件全部运行或存储在极空间 Z4S NAS 上。

- SQLite 数据库文件位于 NAS 的明确持久化目录；
- 媒体位于 NAS 的独立持久化目录；
- 目录通过 Docker bind mount 挂载进容器；
- 数据不得只存在于容器可写层；
- 删除、升级或重建容器后，数据库和媒体必须仍然存在；
- 正式环境不依赖开发电脑；
- 正式数据库从空库开始，不自动展示演示数据或在 API 失败时回退到伪造内容。

关键生产配置必须显式提供。缺失关键配置时应清楚、尽早失败，不应使用一长串掩盖错误的生产默认值。

### 14.2 Render MVP

Render 只承载公开的 MVP 演示版本：

- 可以使用 Render 提供的公开地址、HTTPS 和构建环境；
- MVP 数据属于可丢弃数据，不录入唯一副本或重要正式内容；
- 接受免费实例休眠、冷启动、重启或重新部署导致本地文件丢失；
- 不为 Render MVP 引入第二套外部数据库和对象存储；
- Render 不属于正式持久化架构，也不改变 NAS 使用 SQLite 与本地媒体目录的最终方案。

## 15. 正式部署架构

- 目标设备：极空间 Z4S；
- 使用 Docker/Compose 部署标准 Node.js 应用；
- Web 应用、SQLite 和媒体均在 NAS；
- 外部访问使用域名、Cloudflare DNS 和 Cloudflare Tunnel；
- Tunnel 只提供入站连接方式，不作为数据库、媒体存储或备案规避方案；
- 公开访问必须使用 HTTPS；
- 面向中国大陆与全球访客提供尽力可用的访问体验，不承诺地区 SLA。

## 16. 域名与合规

- 计划在腾讯云购买 `g0dlog.top`，最终价格以购买时腾讯云结算页为准；
- 域名注册后可以把权威 DNS 托管至 Cloudflare；
- MVP 阶段可以指向 Render，正式上线时再切换至 NAS/Cloudflare Tunnel；
- MVP 运行在中国大陆外的 Render 环境；
- 正式从中国大陆家庭网络的 NAS 对公网提供服务前，必须重新确认运营商、域名、备案及其他适用规则；
- 不把 Cloudflare Tunnel 当作绕过监管或运营商规则的手段。

## 17. 技术迁移方向

现有项目是可供参考的 MVP，不绑定其技术实现。后续实施应：

- 迁移到标准、稳定版 Next.js Node.js 运行时；
- 保留 React；
- 保留 Drizzle ORM 和 SQLite；
- 从正式核心架构中移除 Vinext beta、Cloudflare D1 和 R2；
- 将 Cloudflare 限定为 DNS、HTTPS/Tunnel 和适用的边缘功能；
- 对现有大组件和演示 fallback 进行必要整理，避免继续累积原型债务；
- 不为了兼容旧原型而牺牲已确认需求。

具体库和内部模块边界属于实施设计，只要满足本需求、可测试且适合 NAS 长期维护即可。

## 18. 自动部署与发布

GitHub Release 或版本标签是自动部署的明确触发点。发布流程至少包含：

1. 执行 lint、自动化测试和生产构建；
2. 在修改正式环境前备份 SQLite；
3. 获取部署锁，防止并行发布；
4. 执行向前兼容的数据库迁移；
5. 更新应用；
6. 执行健康检查；
7. 健康检查失败时执行经过验证的回滚方案。

MVP 可以通过 GitHub 工作流触发 Render 部署；正式 NAS 可以采用适合 Z4S 的安全拉取或本地执行机制。具体机制在实施阶段确定，但不得要求每次手工替换容器。

计划内更新允许约 1–5 分钟的不可用窗口，应尽量缩短。挂载持久化目录和升级流程不得造成内容丢失。

## 19. 备份、导出与恢复

### 19.1 自动备份

- NAS 内部每天创建数据快照或等价备份；
- 每周将完整备份写入移动硬盘；
- 移动硬盘完成备份后日常保持离线；
- 备份覆盖 SQLite、媒体、恢复所需配置和操作说明；
- 普通故障的数据恢复点目标约为 24 小时；
- NAS 整机灾难的数据恢复点目标约为 7 天。

### 19.2 恢复验证

- 每季度至少执行一次真实恢复演练；
- 演练必须验证数据库可打开、媒体可读取、应用可启动且内容关系正确；
- 不能仅以“备份文件存在”代替恢复验证。

### 19.3 完整导出

系统应支持可迁移的完整导出，至少包含：

- Markdown 正文；
- 文章、专栏和作者相关元数据；
- 历史版本及永久快照；
- 媒体文件；
- 描述关联关系、校验信息和重新导入顺序的 manifest。

应提供与导出格式匹配的重新导入路径，避免数据只能由当前程序读取。

## 20. 日志、审计与告警

- 记录登录、账号管理、权限变更、发布、恢复、删除、永久媒体清除和部署等重要操作；
- 审计日志保留 90 天；
- 日志不得记录密码、会话令牌、SMTP 授权码或完整文章正文等敏感内容；
- 运维告警发送至 `gordon_official@163.com`；
- 邮件发送使用 163 邮箱 SMTP 授权码，凭据只能通过安全配置提供，不得提交到仓库。

## 21. 兼容性与无障碍

- 支持当前主流现代浏览器；
- 公开阅读页必须适配桌面端和移动端；
- 后台允许移动端访问，但编辑效率首先针对桌面端；
- 提供合理的键盘操作、焦点可见性、语义结构和颜色对比度；
- 尊重 `prefers-reduced-motion`，不以动画作为理解或操作页面的必要条件。

## 22. 文档语言

- `README.md` 使用英文；
- `demand.md` 使用中文；
- `docs/deploy-zspace.zh-CN.md` 使用中文；
- 代码、配置名称和技术注释以英文为主；
- 产品界面使用中文。

现有 README 和其他历史文档在实施开始前不要求立即重写；实施时必须同步修正，避免继续描述 Vinext、D1/R2、YiYe 占位品牌或旧编辑器方案。

## 23. 第一版验收标准

第一版只有在以下结果得到验证后才算完成：

### 23.1 功能与权限

1. 访客无需登录即可访问主页、专栏、文章和作者页。
2. Owner 能创建、停用和恢复 Author 账号，并能通过 CLI 离线重置密码。
3. 作者能创建专栏、邀请协作者、创建草稿并直接发布文章。
4. 普通协作者不能修改其他协作者的文章。
5. 移除协作者后，其既有署名和公开文章保留，但编辑权限立即失效。
6. 专栏创建者和 Owner 能管理权限范围内的文章、成员和排序。
7. 草稿、软删除、恢复、永久发布快照和最终删除快照符合本需求。
8. 并发保存不会发生无提示覆盖。

### 23.2 编辑与公开展示

1. 编辑器默认并排显示 Markdown 源码和预览。
2. 自动保存节奏、失败提示和本地草稿恢复有效。
3. Markdown、代码高亮和行内/块级数学公式正确渲染。
4. Markdown HTML 与危险 URL 得到安全处理。
5. 图片上传校验文件魔数和大小，清除元数据并生成展示衍生图。
6. 阅读页在桌面端和移动端均达到正文优先、接近浏览器 PDF 阅读器的简洁体验。
7. 全站及专栏内搜索能够检索中英文内容、显示高亮并遵循约定排序。
8. RSS、Open Graph、sitemap 和 robots 输出正确，且不会泄露受限邮箱或私密信息。
9. URL、重定向和 canonical 在标题或 slug 变化后仍然正确。

### 23.3 部署、数据与恢复

1. lint、测试和生产构建通过。
2. Docker/Compose 可在目标架构上启动，健康检查有效。
3. 删除、重建或升级容器后，NAS 中的 SQLite 和媒体仍然存在。
4. 数据库迁移、失败回滚和部署锁经过验证。
5. 备份可以在隔离环境中实际恢复，导出内容可以重新导入。
6. Render MVP 可以从版本发布自动部署，并明确保持可丢弃数据属性。
7. Cloudflare Tunnel 和公开 HTTPS 完成冒烟测试。
8. 上线前不存在已知高危依赖漏洞。

实际 NAS、正式域名、Cloudflare Tunnel、SMTP 和移动硬盘恢复测试可以作为正式上线前验收项，但不得以本地模拟永久替代。

## 24. 实施授权状态

需求已经确认，但**尚未授权开始实现**。

在站主明确下达开发命令前，只允许进行需求整理、只读分析和方案说明，不应修改应用代码、部署配置、数据库结构或生产环境。
