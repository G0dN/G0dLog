# 极简个人博客与教程站需求文档

## 1. 项目概述

开发一个供站长本人和受邀朋友使用的极简博客站，用于公开展示个人博客、系列文章与教程。

- 当前阶段：仅在本地开发可运行原型。
- 最终部署环境：极空间 Z4S NAS，通过 Docker 运行。
- 公网内容：所有已发布内容均公开访问，不提供私密文章或私密专栏。
- 后台账号：不开放注册，只能由管理员创建并分发给作者。
- 视觉方向：克制、留白充分，公开阅读页接近浏览器 PDF 阅读器，而不是传统资讯门户。
- 站点名称、默认管理员用户名、首页介绍等暂时使用合理的占位内容，并集中配置以便后续替换。

## 2. 用户角色

系统包含以下角色和身份关系。

### 2.1 公网访客

- 无需登录即可访问主页、专栏、文章和作者主页。
- 可以使用全站搜索和专栏内搜索。
- 不能执行任何写操作。

### 2.2 作者

- 账号由管理员创建，不允许自行注册。
- 可以修改自己的显示名称、头像和个性签名。
- 可以创建专栏。
- 可以在自己创建的专栏中管理文章和成员。
- 被邀请后，可以在协作专栏中新建并直接发布文章。

### 2.3 专栏创建者

- 是作者的一种资源级身份，不是独立的全局角色。
- 可以邀请或移除指定作者作为专栏协作者。
- 可以新建、修改、删除和恢复该专栏内的任意文章，包括其他作者创建的文章。
- 可以调整专栏文章顺序。
- 可以删除和恢复自己创建的专栏。

### 2.4 专栏协作者

- 必须由专栏创建者或管理员邀请。
- 可以在已加入的专栏内创建文章，并直接发布。
- 默认只能管理自己创建的文章。
- 被移出专栏后，既有文章继续公开展示并保留原作者署名，但该作者不能再编辑这些文章。
- 被移除后，专栏创建者或管理员仍可修改、删除或移动其既有文章。

### 2.5 管理员

- 可以创建、停用账号和重置作者密码。
- 可以管理所有用户、专栏、成员关系、文章、媒体和历史版本。
- 可以邀请或移除任意专栏协作者。
- 可以修改、删除和恢复任何文章或专栏。
- 可以查看所有已删除内容。

## 3. 账号与个人资料

### 3.1 账号字段

- 内部用户 ID：系统生成、稳定且不可修改，用于所有数据关联。
- 登录用户名：由管理员创建，默认不可由作者自行修改。
- 显示名称：作者可修改，用于公网展示。
- 头像：作者可上传和修改。
- 个性签名：作者可修改。
- 账号状态：启用或停用。
- 密码：管理员创建初始密码；作者首次登录必须修改密码。

### 3.2 账号生命周期

- 账号不进行物理删除，只能停用。
- 停用后用户无法登录。
- 停用不会影响其已发布文章、专栏和公网作者资料。
- 管理员可以重置密码。
- 第一版不做自行注册、邮箱验证、邮箱找回密码和双重验证。

### 3.3 公网作者主页

点击文章作者可进入作者主页，展示：

- 头像；
- 显示名称；
- 个性签名；
- 该作者创建的公开专栏；
- 该作者发布的公开文章。

## 4. 公网主页

### 4.1 专栏列表

- 主页展示所有公开且未删除的专栏。
- 默认按专栏最近一次发布文章的时间从新到旧排列。
- 没有已发布文章的专栏不在公网主页展示。
- 专栏卡片至少展示：专栏标题、简介、创建者、最近发布时间和公开文章数量。

### 4.2 全站搜索

- 主页顶部提供全站搜索框。
- 一次搜索同时匹配文章标题、文章正文、专栏和作者。
- 搜索结果分为“文章”“专栏”“作者”三类。
- 文章正文命中时，展示关键词附近的摘要。
- 搜索只返回公开且未删除的内容。
- 中文和英文内容均应可搜索。

## 5. 公网文章阅读页

### 5.1 页面结构

整体体验接近浏览器内置 PDF 阅读器：界面克制，重点突出正文。

- 左上角：返回主页。
- 左侧：当前专栏内所有已发布、未删除文章的目录。
- 中间：渲染后的 Markdown 正文。
- 右上角：当前专栏搜索框。
- 正文信息：文章标题、作者、首次发布时间和最后更新时间。
- 文章目录：根据 Markdown 标题自动生成，可用于页内跳转。

### 5.2 专栏文章顺序

- 默认按文章首次发布时间从早到晚排列。
- 文章后续编辑不会改变首次发布时间和默认位置。
- 专栏创建者和管理员可以手动拖拽调整固定顺序。
- 一旦手动排序，阅读页左侧目录应遵循保存后的顺序。

### 5.3 专栏内搜索

- 只检索当前专栏中的公开文章。
- 同时匹配文章标题、正文和作者。
- 不返回其他专栏内容。
- 正文命中时展示关键词附近的摘要。

### 5.4 响应式行为

- 桌面端固定显示左侧文章目录。
- 手机端将左侧目录折叠为可打开的目录按钮或抽屉。
- 返回主页和搜索入口在手机端仍应易于访问。
- 后台允许在手机端打开，但第一版主要优化桌面编辑体验。

## 6. 专栏管理

专栏至少包含以下字段：

- 稳定且不可修改的内部 ID；
- 系统生成的公开地址标识；
- 标题；
- 简介；
- 创建者；
- 创建时间；
- 最近公开文章发布时间；
- 删除状态及删除时间；
- 手动排序所需数据。

专栏功能：

- 作者可以创建专栏。
- 专栏创建者和管理员可以修改专栏资料。
- 专栏创建者和管理员可以从现有启用账号中邀请协作者。
- 专栏创建者和管理员可以移除协作者。
- 专栏创建者和管理员可以调整文章顺序。
- 删除专栏后，专栏及其文章立刻停止公开展示，但所有内容继续保留。
- 删除专栏不能触发文章、版本或图片的物理删除。

## 7. 文章管理

### 7.1 文章字段

文章至少包含：

- 稳定且不可修改的内部 ID；
- 系统自动生成的公开地址标识，作者不能修改；
- 所属专栏；
- 作者；
- 标题；
- Markdown 正文；
- 状态；
- 首次发布时间；
- 最后更新时间；
- 目录排序值；
- 删除状态、删除人和删除时间；
- 并发修改检测所需的版本号或更新时间戳。

### 7.2 状态

- 草稿：仅有权限的后台用户可见。
- 已发布：公网可见。
- 已删除：立即停止公网和普通后台列表展示，但数据永久保留。

文章必须支持从草稿发布、已发布内容更新、删除和恢复。

### 7.3 权限

- 文章作者可以管理自己的文章，但必须仍拥有所属专栏的访问权限。
- 专栏创建者可以管理专栏内的任意文章。
- 管理员可以管理任意文章。
- 普通协作者不能修改或删除其他协作者创建的文章。
- 协作者被移出专栏后不能继续修改既有文章。

### 7.4 删除与恢复

- 删除操作本质为永久软删除。
- 删除后立即停止公网展示。
- 文章正文、元数据、历史版本和关联图片不得物理删除。
- 作者可以恢复自己删除且仍有权限访问的文章。
- 专栏创建者可以恢复专栏内文章。
- 管理员可以查看和恢复全部删除内容。

## 8. Markdown 编辑器

### 8.1 交互方式

- 使用类似 Typora 的单页面所见即所得体验。
- 不使用左右双栏编辑与预览。
- Markdown 标记输入后应在同一编辑区域内即时渲染。
- 提供源码模式作为复杂格式或异常内容的备用编辑方式。
- 自动保存草稿。
- 页面关闭、刷新或跳转前，如仍存在未完成保存，应明确警告。

### 8.2 Markdown 能力

第一版支持：

- 标题和自动文章目录；
- 段落、链接、引用和常用行内格式；
- 代码块与代码高亮；
- 表格；
- 图片。

第一版不要求：

- 原始 HTML；
- Mermaid；
- 数学公式；
- PDF 或通用附件上传。

为避免跨站脚本风险，渲染输出必须经过安全过滤。

### 8.3 图片上传

- 支持粘贴图片。
- 支持拖拽图片和文件选择上传。
- 支持 JPG、PNG、WebP 和 GIF。
- 单张图片最大 10 MB。
- 上传后由系统生成安全且唯一的文件名。
- 图片文件保存到 NAS 的独立持久化媒体目录。
- 删除文章、移除引用或淘汰历史版本时，均不得自动物理删除图片。

## 9. 自动保存、版本历史与并发编辑

### 9.1 自动保存

- 编辑过程应定期自动保存。
- 明确显示“正在保存”“已保存”和“保存失败”等状态。
- 保存失败不得静默丢失当前内容。

### 9.2 版本历史

- 每篇文章默认保留最近 20 个保存版本。
- 管理员以后可以调整保留数量。
- 有权限的用户可以查看历史版本并恢复。
- 已删除文章的最终内容必须永久保留。

### 9.3 并发冲突

- 两名有权限的用户同时编辑同一篇文章时，不允许后保存者静默覆盖新版本。
- 保存时使用版本号或更新时间戳进行冲突检测。
- 检测到冲突后，应保留当前用户输入，提示内容已被他人更新，并允许复制当前内容后重新加载最新版。

## 10. 搜索规则

- 搜索必须覆盖中文和英文。
- 全站搜索范围：公开文章标题、公开文章正文、公开专栏和公开作者。
- 专栏内搜索范围：当前专栏的公开文章标题、正文和作者。
- 已删除、草稿、停用后未公开的资源不得进入搜索结果。
- 预计规模小于 20 位作者、5,000 篇文章、日访问量低于 10,000，因此第一版不强制部署独立搜索服务。
- 实现时必须验证中文全文搜索质量；若所选数据库的默认分词无法满足要求，可使用适合小规模数据的规范化包含搜索或轻量级索引方案。

## 11. URL 与稳定性

- 用户、专栏和文章均使用不可变内部 ID 建立关联。
- 文章公开 URL 由系统自动生成，作者不能修改。
- 修改文章标题后，既有 URL 不能失效。
- 专栏和作者显示名称变化后，既有数据关联不能失效。
- 如果 URL 中包含可读文本，仍必须包含或映射到稳定 ID。

## 12. 数据持久化与备份原则

- 结构化数据、Markdown、历史版本和图片必须持久化，不能只保存在浏览器中。
- 删除任何业务内容都不得触发物理删除。
- 应区分数据库数据目录和媒体文件目录，并通过 Docker volume 映射到 NAS 明确位置。
- Docker 容器被删除或升级后，内容必须仍然存在。
- 需要提供可执行的备份方案，至少覆盖数据库、Markdown 内容和媒体目录。
- “内容永久保留”不等于仅存一份；NAS 硬盘故障仍可能导致数据丢失，因此正式部署前必须配置第二份备份。

## 13. 部署要求

### 13.1 当前本地原型

- 在本地开发和验证，不依赖当前不可用的 NAS 连接。
- 原型应覆盖核心页面、权限、编辑、搜索、上传和持久化流程。
- 使用占位站点名称、管理员用户名和首页介绍。

### 13.2 最终 NAS 部署

- 目标设备：极空间 Z4S。
- 使用 Docker Compose 启动。
- 推荐组成：一个 Web 应用服务、一个持久化数据库或数据库文件、NAS 本地媒体与数据目录。
- 配置项应通过环境变量或集中配置提供，不能硬编码密码和 NAS 路径。
- 后续再配置域名、HTTPS 反向代理和自动备份。
- 项目应提供简洁的中文部署说明，面向不熟悉 Docker 的使用者。

## 14. 第一版明确不做

- 评论；
- 点赞；
- 关注；
- 邮件通知；
- 访问统计；
- 私密专栏或私密文章；
- 多语言界面；
- 开放注册；
- 邮箱找回密码；
- 双重验证；
- PDF 或其他通用附件；
- Mermaid 与数学公式。

## 15. 核心验收标准

满足以下条件才算第一版本完成：

1. 公网访客能从主页进入专栏和文章，无需登录。
2. 主页专栏按最近发布文章时间正确排序。
3. 阅读页具备返回主页、专栏目录、Markdown 正文和专栏内搜索。
4. 手机端可通过折叠目录正常阅读和搜索。
5. 管理员能创建、停用账号并重置密码。
6. 作者首次登录被要求修改初始密码。
7. 作者能创建专栏并邀请指定用户协作。
8. 协作者能在被邀请专栏中创建、自动保存并直接发布文章。
9. 普通协作者不能修改或删除其他协作者的文章。
10. 专栏创建者和管理员能管理专栏内任意文章。
11. Markdown 编辑器为单页面即时渲染，支持代码、表格、目录和图片。
12. 图片可通过粘贴上传，格式和 10 MB 大小限制有效。
13. 全站搜索和专栏内搜索均能正确检索中文标题与正文。
14. 文章默认按首次发布时间从早到晚排列，并可手动调整。
15. 删除文章、专栏或账号相关资源后，公网立即不可见，但后台可恢复且磁盘内容未被物理删除。
16. 最近 20 个文章版本可查看和恢复。
17. 并发编辑不会发生无提示覆盖。
18. 修改标题、作者显示名称或专栏名称不会破坏既有 URL 和数据关联。
19. 本地重启服务后账号、文章、版本和图片仍然存在。
20. Docker Compose 配置能够映射 NAS 持久化目录，并附带中文部署说明。

## 16. 推荐实施顺序

1. 数据模型、账号认证、权限检查和软删除机制。
2. 公网主页、专栏阅读页、作者页和基础响应式布局。
3. 后台专栏、成员和文章管理。
4. Typora 式 Markdown 编辑、自动保存、图片上传和版本历史。
5. 全站搜索与专栏内搜索。
6. 并发冲突处理、恢复流程和安全加固。
7. Docker Compose、持久化卷、备份说明和极空间 Z4S 部署文档。

## 17. 实现约束提醒

- 所有权限必须在服务端校验，不能只隐藏前端按钮。
- 密码必须使用成熟的密码哈希算法保存，不能保存明文。
- 上传文件必须验证真实类型、大小和安全文件名。
- Markdown 输出必须进行安全过滤。
- 所有数据库查询和上传接口应防止注入、越权和路径穿越。
- 已删除内容默认不得被普通查询、搜索结果或公开接口返回。
- 任何清理任务都不得删除文章正文、历史版本或媒体文件。

